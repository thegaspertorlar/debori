# Vite + TypeScript App

This repository contains a minimal Vite application using TypeScript.

Available scripts:

- npm run dev — starts the development server
- npm run build — builds the production bundle to dist/
- npm run preview — locally preview the production build

To get started:

1. npm install
2. npm run dev
